import EnterComponent from "@/components/layout/enter/enterComponent";
import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Đăng nhập",
};

export default function Signin() {
    return <EnterComponent />;
}
